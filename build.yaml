---
include:
  - board: eyelash_corne_left
    shield: nice_epaper
  - board: eyelash_corne_left
    shield: nice_epaper_new
    #snippet: zmk-usb-logging    # Uncomment to log over USB.
  - board: eyelash_corne_right
    # No display shield: the Azoteq touchpad replaced the right-half OLED.
    # Touchpad/I2C come from config/eyelash_corne.conf; display is turned off
    # for this half by config/eyelash_corne_right.conf.
    #snippet: zmk-usb-logging
  # - board: eyelash_corne_left
  #   shield: nice_view_custom
  #   snippet: studio-rpc-usb-uart
  #   cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
  #   artifact-name: eyelash_corne_studio_left
  - board: eyelash_corne_left
    shield: settings_reset
    #snippet: zmk-usb-logging
 
